"use client"

/**
 * @deprecated Use useFeatureFlag() from hooks/use-feature-flag.ts instead.
 * Kept for backward compatibility.
 */
export { useFeatureFlag as useFeature, useFeatureFlags as useFeatures } from '@/hooks/use-feature-flag'
